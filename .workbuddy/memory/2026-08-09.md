# 2026-08-09

## 修复：大盘走势图数据不显示（dev 分支）
- 根因1（主源）：`server/routes/indices.js` 的 `parseTencentMinuteData` 未兼容腾讯分钟接口新返回的三层嵌套 `{data:{data:[...]}}`，静默返回 null → 前端显示“暂无分时数据”。
- 根因2（降级源）：`generateFallbackIntraday` 与 `parseTencentRealtimeSnapshot` 读取 `qt.gtimg.cn` 实时行情字段错位：`open` 误用代码字段 `fields[2]`、`preClose` 误用涨跌幅 `fields[32]`。正确布局（A股/港股/美股通用）：`f[3]=current, f[4]=preClose, f[5]=open, f[31]=涨跌额, f[32]=涨跌幅%, f[33]=high, f[34]=low`（参考同文件 `pT()`）。
- 修复提交（已在 dev）：`c152a628`（cherry-pick 之前被 reset 丢弃的修复：主源解析 + 前端 tencent_minute 标签）、`4ee40b9d`（降级源字段索引修正 open=f[5]/preClose=f[4]）。
- 验证：清掉 `server/data/globalCache.json` 的 `indices:intraday:*` 缓存后重启，000001/399001/399006 经 `tencent_minute` 返回 ~230 个不同价格；HSI/DJI/HSTECH 经 `tencent_snapshot_realparams` 由 2~8 个取值提升至 180~230 个，曲线正常。
- 坑：验证时旧 server 进程（port 3001）未被 `pkill` 杀掉导致 EADDRINUSE、新代码未加载；Git Bash 的 `kill` 无法杀 Windows PID，需用 PowerShell `Stop-Process -Id <pid> -Force`。此外旧 server 会把坏数据重新写回缓存，必须杀掉旧进程并清空 intraday 缓存再重启才能验证修复生效。
- 未提交：admin-dist.tar/admin.zip 的删除、server/data/ 缓存、web/src/vite-env.d.ts（按计划保持未跟踪/未提交）。

## 优化：大盘走势图 X 轴时间显示（dev 分支）
- 文件：`web/src/pages/market/MarketDetailPage.tsx`（图表组件）。
- 后端 `times` 为 242 个无冒号字符串（如 `'0930'`），原桌面端 `axisLabel.interval:0` 会把 242 个标签全量画出，拥挤重叠。
- 改动：新增 `xAxisTimeFormatter`，将 `'0930'`→`'09:30'`，且仅在开盘 09:30/整半点/上午收盘11:30/下午开盘13:00/收盘15:00 显示，其余返回空字符串隐藏；`xAxis.boundaryGap:false` 使时间轴从 09:30 左缘连续到 15:00 右缘；`hideOverlap + showMinLabel/showMaxLabel` 防首末标签裁切；series 加午间休市虚线 `markLine(xAxis:'1130')` 区分上午/下午。
- 效果：X 轴只显示 10 个关键时点标签。提交 `ae3ace81`（仅该文件，dev）。

## 修复：登录报「服务器内部错误」/ 所有 DB 接口 500
- 根因：`server/app.js` 原 `require('dotenv').config()` 只从 cwd（realtime/）加载 `.env`，而真实数据库配置在 `server/.env`（连远程 MySQL `47.106.91.127:3306`，库名 `real_time`）。环境变量从未被加载 → mysql2 回退连 `localhost:3306`（本机无 MySQL）→ 所有 DB 查询 ECONNREFUSED → 登录等接口 500。
- 修复：`app.js` 第1行改为 `require('dotenv').config({ path: require('path').join(__dirname, '.env') })`，显式加载 `server/.env`；重启后端。验证：启动日志出现 `MySQL 连接建立 (1/10)`；POST /api/auth/login 用假账号返回 400「用户名或密码错误」（DB 查询成功、不再 500）。
- 注意：远程 MySQL 主机可达（TCP 3306 通），是 env 未加载而非网络故障。今后启动后端务必用修正后的 app.js，不要从其他 cwd 直接 `node app.js` 而依赖隐式 .env。

## 修复：行情(行业轮动)界面「数据加载失败，所有数据源均不可用」
- 现象：`/api/market/rotation` 返回 200 但 indices=0、sectors=0，前端 `useMarketData` 仅在 `indices.length||sectors.length>0` 时认为成功，否则抛「所有数据源均不可用」。
- 根因：`server/services/quotes.js` 的指数与行业板块**单点依赖东方财富 `push2.eastmoney.com`**，该源在本环境被限流（`fetch failed` / `kamt` 返回 `rc:102`），且 `Promise.all([fetchIndices,fetchSectors])` 任一项抛错即把两者一起置空；东财一旦全挂，整页无数据。
- 修复：
  1. `fetchIndices` 改为**腾讯 `qt.gtimg.cn` 优先（稳定）+ 东财兜底补齐缺失项**（复用 indices.js 的 pT 解析，name 用静态 INDEX_DEFS 表）。
  2. `fetchSectors` 改为东财多 host 兜底（`push2.eastmoney.com` → `push2delay.eastmoney.com`），**全部失败返回 [] 不抛出**。
  3. `getRotationData` 中四项（indices/sectors/breadth/northbound）改为**各自独立 try/catch**，单项失败不再连累其他。
- 验证：干净重启后端后 `/api/market/rotation` 返回 indices=10（腾讯真实数据）、sectors=80（东财真实数据）、northbound.series=9；前端不再报错。breadth 涨跌家数仍 0/0/0（依赖东财 stock/get，被限流，已知降级）。

## 集成：把「A股行业轮动与资金流向监控仪表盘.html」整屏移植进「行情」（数据动态化）
- 用户要求将该 HTML 整屏集成到行情 tab，且数据动态（非静态快照）。
- 新增 `web/src/features/industry-rotation/RotationTerminal.tsx` + `rotationTerminal.css`（`.rt-root` 作用域隔离，暗色终端风，响应式）：完整还原 HTML 结构——头部/指数卡/统计条/资金主线/热力网格(核心16/全部切换)/资金排行(线性/√切换)/风格矩阵+剪刀差/北向SVG图+沪深股通十大成交股(按 sh/sz 分表)/3条研判/来源。
- **动态生成**：「今日资金主线」与「3 条行业配置研判」由实时数据自动汇总（buildNarrative/buildThesis），不再写死历史文案。
- 页面 `IndustryRotationPage.tsx` 改为 `useMarketData` 直连 `/api/market/rotation` 并渲染 `RotationTerminal`；`index.ts` 导出 `RotationTerminal`。
- 后端 `quotes.js` 增强：`fetchStyleMatrix` 用**腾讯 `sz399372-399377` 优先 + 东财兜底**填充巨潮 6 风格（styleMatrix）；北向十大成交股改带 market/amount/pct（东财 `fs=m:1+t:23`/`m:0+t:23` 实时 + 基线兜底），前端按 sh/sz 分表。
- **踩坑**：① 巨潮 code→name 我最初写反（399372 实为大盘成长不是大盘价值），修正后 6 值与 HTML 完全吻合；② 腾讯指数成交额字段为 f[37] 且量纲异常，故指数成交额改由东财兜底（沙箱限流时两市成交额显示 `--`，本机正常）。
- 验证：`tsc` 本次新增文件零错误（仅预存 Header/PortfolioPage 无关错）；Vite 转译 RotationTerminal 成功(180KB JS)；后端 styleMatrix=6 名称数值正确、sectors=80、top10 含 sh/sz。
- 注意：公开行情源有频率限制，压测会触发限流；东财挂掉时至少指数仍由腾讯兜底，页面不会整体失败。

## 优化：盘中固定显示完整 09:30–15:00 全刻度（dev 分支）
- 追加改动 `web/src/pages/market/MarketDetailPage.tsx`：用户要求盘中就固定显示完整 09:30–15:00 刻度、曲线只填到当前、右侧留白。
- 新增 `buildFullTradingGrid(code)`：A 股（000001/000016/399001/399006/000300/000688/399673/000905/000852）返回 `'HHMM'` 格式完整刻度（09:30–11:30 + 13:00–15:00，含午休缺口，共 242 格）；港股/美股返回 null（沿用后端原始数据，不套用网格）。
- 渲染时把收到的分时数据（盘中只到当前分钟）按时间去冒号归一化映射到固定网格，未来时段填 `null` → 曲线止于当前、右留白。兼容主源 `'0930'` 与降级源 `'09:30'` 两种格式。
- tooltip 对尚未走到的时段返回"尚未走到的时段"提示；数据点徽标改用真实采样点数 `realPointCount`。
- 验证：模拟 11:00 截断数据 → 固定网格 242 格、真实点 91、空 151、X 轴恒显 10 标签。提交 `b0e44bd2`（仅该文件，dev）。
- 开发环境：前端 Vite `:5173`（已运行，HMR 直接读源码，代理 /api → 后端 `:3001`）。注意 `.bin/vite` 是 shell 脚本，不能用 `node` 直接跑；正确启动为 `node node_modules/vite/bin/vite.js --host 0.0.0.0 --port 5173`。

## 修复：港股分时图伪造曲线（dev 分支，提交 2d50d684）
- 用户反馈"港股美股看着不对"。核查确认：HSI/DJI/HSTECH 原 source=`tencent_snapshot_realparams`，来自 `parseTencentRealtimeSnapshot`（indices.js ~555-641 行），该函数用 `开盘价+总涨跌×进度 + sin/cos 数学波动 + Math.random()` 伪造 240 个点，**每次刷新曲线都变**，且非真实逐分钟数据。
- 根因：handler 只对 A 股（`TX_A`）调腾讯真实分时 `parseTencentMinuteData`；港股美股走 `TX[code]` 分支，先调日K线（`ifq.gtimg.cn` day kline，非分时）失败后才落伪造快照。
- 腾讯港股真实分时接口可用：`web.ifzq.gtimg.cn/appstock/app/minute/query?code=hkHSI` 返回真实逐分钟（`data.data.data` 数组，交易日全天 332 点，含 16:00）。`parseTencentMinuteData` 已能解析。
- 改动：handler 对 `TX[code]` 中 `hk` 前缀优先调腾讯真实分时；`parseTencentMinuteData` 取数 key 由 `TX_A[code]` 扩展为 `TX_A[code]||TX[code]`（港股 key 在 TX 映射）。美股（us 前缀）因腾讯美股分时历史日仅返回 1 点，保留日K→快照兜底。
- 验证：HSI/HSTECH 修复后 source=`tencent_minute`、332 点、波动自然（真实）；DJI 仍 `tencent_snapshot_realparams`（保留兜底，符合用户选择）。
- 待办：美股真实分时源未解决（腾讯美股分时历史日单点）；港股午休 12:00（非 A 股 11:30），前端午间 `markLine('1130')` 对港股位置略有偏差，可后续优化。

## 修复：美股真实源代码就位 + 港股午间线 + 模拟标注（dev 分支）
- 美股真实源调研：新浪 US_MarketData 返回 {"__ERROR":3,"Service not valid}（接口失效）；东方财富 push2his 沙箱网络层 fetch failed（仅腾讯 web.ifzq.gtimg.cn/qt.gtimg.cn 可达）；腾讯美股分时 usDJI 结构正确但当前(周日)仅返回1点(1650)，不像港股返回完整交易日。→ 当前环境美股真实分时源不可靠获取。
- 后端(11b6eb57)：handler 对 TX[code] 的 hk/us 前缀均优先腾讯真实分时(parseTencentMinuteData 解析)；新增点数保护 times.length<30 视为无效降级日K→快照。当前美股单点触发降级(仍 snapshot)，交易日若腾讯补全美股分时则自动生效。
- 前端(0dc9b1ff)：新增 getLunchBreak(code) 港股12:00/A股11:30/美股null 午休；markLine 午间线改用该值，修正港股错标11:30。getSourceLabel 将 tencent_snapshot_realparams/realtime_params_fallback 由误导"实时参数"改为"⚠️ 模拟数据(非真实行情)"，诚实告知曲线为 OHLC+随机数生成。
- 验证：HSI/HSTECH tencent_minute 332点；DJI/IXIC 仍 tencent_snapshot_realparams(降级)；前端Vite编译通过。
- 待办：美股真实源仍需交易日验证腾讯是否补全；若要美股真实数据需另找源(新浪/东财需解决网络或接口失效)或接盘前盘后接口。

## 修复：彻底移除模拟数据，无真实分时显示"暂无数据"（dev 分支，提交 6c8137b9）
- 用户明确："不要模拟数据，没有就直接显示没有"。
- 改动 `server/routes/indices.js`：移除三处伪造分时对外调用——`parseTencentKlineHistory`(历史日K线当日线，粒度误导)、`parseTencentRealtimeSnapshot`(OHLC+Math.random 合成)、`generateFallbackIntraday`(OHLC+随机噪声合成)；三函数体均改 `return null`（现死代码，后续可删）。handler 仅接受真实逐分钟分时(点数>=30)；点数过少/无数据不再降级到快照/模拟。
- 无数据时接口由 HTTP 500 改为 HTTP 200 + { data:null, source:'none', pointCount:0 }，前端据此显示"暂无分时数据"(MarketDetailPage 行 576，loadIntradayData 行110 置 null)。
- 验证（重启清缓存后）：000001/399001 tencent_minute 242点(真实)；HSI/HSTECH tencent_minute 332点(真实)；DJI/IXIC source=none、0点 → 前端"暂无分时数据"。通过 Vite 代理(5173→3001)同样生效。
- 坑：重启时旧 server(PID 26164)仍占 3001 跑旧代码，新进程 EADDRINUSE 崩溃；netstat 偶发误报 FREE。需 PowerShell Stop-Process 杀掉旧 PID 再启动，并清 intraday 缓存。

## 集成：A股行业轮动仪表盘（fund-integration → 行情 Tab）
- 来源包：`WorkBuddy/2026-08-09-01-00-37/fund-integration/`（React18+TS 组件库，零运行时依赖）。整目录拷贝到 `web/src/features/industry-rotation/`，前端技术栈（React18.2/TS/Vite/@别名/react-router/antd图标）完全匹配。
- 入口：底部 Tab `BottomTabBar.tsx` 新增「行情」（icon `DotChartOutlined`），插在「自选」后；`App.tsx` 懒加载 `pages/market/IndustryRotationPage`，路由 `/market/rotation`。页面 `primaryUrl:'/api/market/rotation'`、`fallback:null`、`pollIntervalMs:30000`。
- 后端：`server/services/quotes.js`（聚合 DashboardData：东财 push2 抓宽基指数+行业板块m:90+t:2+涨跌家数；北向 kamt 最佳努力+静态基线）、`server/routes/market.js`（GET /rotation）、`server/utils/http.js`（`fetchWithTimeout` 抽公共）。`app.js` 挂载 `/api/market`。`indices.js` 改为复用 `../utils/http` 的 `fetchWithTimeout`（行为不变）。
- 缓存对齐：轮动接口走 `globalCache.getOrFetch(key,{type:'realtime'})`，与基金实时数据（持仓/指数接口，盘中 28s）**共用同一 getRealtimeTTL()**，刷新节奏一致；前端 30s 轮询≈缓存过期后再拉。
- 主题：`features/industry-rotation/theme.css` 重写，把 `--ird-*` 映射到 App 语义变量（`--bg-elevated/--accent-gold/--gain/--loss` 等），自动跟随 `[data-theme]` 明/暗切换；热力单元格保留 A股涨红跌绿 literal rgba。
- 验证：`/api/market/rotation` 返回合法 DashboardData（HTTP200，thesis:3/notes:4/结构正确）；`/api/indices` 回归正常（000001/000300/399001 实时数据）。**沙箱 IP 被东财限流**导致 indices/sectors 暂时为 0（早前隔离测试已返回 10 指数+80 行业真实数据证明代码正确），用户真实环境正常。
- 已知缺口（回退源限制，符合计划）：巨潮 6 风格 styleMatrix=[]、北向日净买入 2024-08-19 起停披（仅成交额趋势+十大成交股）、主力资金统一东财单口径、行业板块为东财分类（非申万一级，CORE16 切换按钮退化为显示前 16 个东财板块）。
- 前端 tsc 检查：本次新增/改动文件无错误；库内仅 `Header.tsx`/`PortfolioPage.tsx` 有预存的 `SettingsData.settings` 类型错误（与本集成无关）。
- 运维：Git Bash `kill` 杀不掉 Windows PID（EADDRINUSE 残留）；本次用 `taskkill /F /PID <pid>` 成功杀掉旧 3001 进程并重启。`pkill` 对 MSYS 子进程有效、对独立 Windows PID 无效。

## 优化：行情行业轮动终端 手机端可收起面板
- 新增 `Panel` 封装组件（`RotationTerminal.tsx`），替代原先散落的 `div.panel > div.ph + div.pb`。
- 行为：桌面端始终展开、无箭头；手机端（≤768px）每面板头部右侧出现箭头按钮，点击收起/展开。收起态 `.closed` 仅在手机断点内隐藏 `.pb`，旋转到桌面内容始终可见、状态不卡死。
- 默认手机端仅展开「大盘概览」「热力网格」，其余（资金排行/风格/北向/研判/来源）默认收起，降低长屏滚动。
- 箭头显隐与包裹换行逻辑放在 `rotationTerminal.css` 的 `.rt-chev` / `.ph-l` / `.ph-r` 及 `@media(max-width:768px)` 内。
- tsc 本次改动零新错误（仅 `Header.tsx`/`PortfolioPage.tsx` 预存 `settings` 无关错）。

## 优化：行情终端 一键展开全部 + 尺寸压缩
- `Panel` 改为受控：开合状态提升到 `RotationTerminal` 的 `openMap`（键 overview/heat/flow/style/north/thesis/source）。桌面恒 true；手机默认仅 overview+heat 展开。
- 头部下方新增「展开全部 / 收起全部」按钮（`.rt-expandall`/`.rt-eab`），仅 ≤768px 显示，点击切换所有面板；各面板自身箭头仍可单独收起/展开。`allOpen` 由 `PANEL_IDS.every` 计算。
- 移动端尺寸整体压缩：根 padding、header、panel/ph/pb、指数卡、统计条、热力卡、风格矩阵、北向、研判字号与内边距均下调；箭头 24px；480 档进一步收窄资金排行列宽（58/1fr/64）。
- tsc 零新错误，Vite(5173) 在线（200）。

## 优化：行业轮动(行情)终端 —— 跟随黑白主题 + 手机端排版
- 诉求：①界面排版优化；②跟随 App 的黑白(暗/亮)风格切换；③重点手机端。
- 主题机制：App 用 `document.documentElement[data-theme]`(dark/light) + `useThemeStore`，语义变量定义在 `App.css`(`:root`=亮、`[data-theme="dark"]`=暗)。
- 改动 `rotationTerminal.css`：`.rt-root` 局部变量全部映射到 App 语义变量(`--bg-base/--bg-card/--border-*/--text-*/--gain/--loss/--accent-gold`)，涨红跌绿 `--up→--gain`/`--dn→--loss`；强调描边/底色统一用 `color-mix(in srgb, <语义变量> %, transparent)` 自适应，避免亮色过曝或不可见。整页自动随主题切换。
- 改动 `RotationTerminal.tsx`：引入 `useThemeStore` 取 `isLight`，让**亮色下文字可读**——①热力网格 cell 底色+文字色由 `heatColor(p,isLight)` 返回(bg+fg：暗态白字 / 亮态深红深绿字)；②风格矩阵 cell 底色 `styleBg(p,isLight)` 亮态降 alpha；③北向 SVG 网格/轴/点/标签色由 `nbColors`(明/暗两套)注入；④研判序号底色用 `var(--upBg)/--am-dim/--dnBg` + 文字 `var(--up)/--am/--dn`。
- 手机端(≤768 / ≤480 两档)：头部 flex 改纵向堆叠、隐藏副标题；指数卡 6→4→3→2 列；热力网格 4→3→2 列且字号/内边距压缩；资金排行固定列宽 74/84 收窄到 64/70；风格矩阵标签 52→46→40；剪刀差标签 132→104→92；统计条 5→3→2 列；内边距逐级压缩；图例色条宽度受限防横向溢出。配合 `App.css` 全局 `overflow-x:hidden` 杜绝横向滚动。
- 验证：`tsc --noEmit` 本次改动零新错误(仅预存 Header/PortfolioPage 的 `SettingsData.settings` 无关错)。Vite(5173) HMR 直接读源码，刷新行情页即生效。
- 注：`color-mix` 需现代浏览器(Chrome111+/Safari16.2+/FF113+)，本工程为现代 React/Vite，兼容无忧。

## 优化：行情界面手机端交互(UI 设计师视角)
- 新增**移动端吸顶命令栏**(`nav.rt-mnav`，仅 ≤768 显示，`position:sticky;top:0` + 玻璃质感 `backdrop-filter:blur` + 底边框/阴影)：
  - 上排：品牌「行情」+ 实时点 + 更新时间 + (刷新按钮 `rt-rf`，带 spin 动画) + 展开全部/收起全部(`rt-eab`)。
  - 下排：横向可滚动 **区块导航 chips**(概览/热力/资金/风格/北向/研判/来源)，点按 `scrollIntoView` 平滑滚动并**自动展开**对应面板；用 `IntersectionObserver`(rootMargin `-96px 0 -65%`)在手动滚动时同步高亮当前区块。
- 数据 hook `useMarketData` 新增 `refresh/refreshing`(复用内部 `tick`)，页面 `IndustryRotationPage` 透传 `onRefresh/refreshing` 给终端。
- 标题移动端缩写：长标题 `.full` 隐藏、显示 `.mini`「行情 · 行业轮动」。
- 触控目标放大：chevron 24→30px、seg 按钮高 32px、chip 高 32px、刷新/展开按钮 34px——符合 ≥44px 舒适区(视觉控制在 30–34，布局紧凑)。
- `scroll-margin-top:108px` 防止锚点跳转被吸顶栏遮挡；新增 `prefers-reduced-motion` 关闭动画。
- 桌面端：mnav 隐藏、面板恒展开，行为不变。验证：tsc 本次零新错，Vite 200。

## 重构：行情界面改为标签式工作台(UI 设计师视角,突破旧排版)
- 彻底重构信息架构：弃用「7 个可折叠长滚动面板」，改为吸顶标签栏工作台 rt-tabs(概览/行业/风格/北向/研判/说明)。
- 交互：.rt-sec{display:none} + .active{display:block}，全尺寸标签切换(移动端单视图聚焦消灭长滚动；桌面端标签内双栏 g-main 保持密度)。
- 概览新增市场温度仪表 rt-gauge(涨跌家数60%+上涨行业40%启发式,0-100°,偏热红/偏冷绿/中性金) + 指数横向滑动卡 rt-idxscroll(移动端 overflow-x 横滑)。
- 移除旧 Panel/IntersectionObserver/scrollToPanel/expandAll 复杂度；刷新按钮移入吸顶栏右侧常驻。
- 顶栏 rt-head(品牌+日期+live时间) + 吸顶 rt-tabs(glass blur) 两级；tab 用金/青渐变高亮。
- 手机端：hero 纵向堆叠(仪表变横向行)、网格列数收缩、内边距压缩、横滑卡与标签栏横向滚动。
- 验证：tsc 本次零新错(仅两预存无关错)，Vite 200，HMR 生效。

## 优化：概览指数可点击 + 显示优化(UI 设计师)
- 概览指数卡片改为**可点击按钮**，跳转 `/market?code=<code>`，与持仓界面 MarketIndexStrip 行为完全一致（命中 MarketDetailPage 分时详情）。
- `RotationTerminal.tsx`：引入 `useNavigate`，指数 chip 改为 `<button>` 配 `aria-label`、`onClick→navigate`。
- 视觉优化：指数列加标题「主流指数 / 点击查看分时 ›」提升可发现性；每张卡加「分时 ›」箭头提示 + 悬停上浮 + 按压缩放 + focus 环；升级为 flex 纵向卡片（名称/点位/涨跌%/进入），min-width 118、min-height 60、亮暗均跟随。
- 研判是实时生成：3 条由 `buildThesis(data)` 在 `useMemo(...,[data])` 中据当前实时 `data` 计算，数据每 30s(`pollIntervalMs`)从 `/api/market/rotation` 重拉即重算，非写死。
- 验证：tsc 本次零新错；Vite 200。

## 优化：指数详情页分时图 手机端 y 轴太窄(UI 设计师)
- 根因：手机端 `grid.left` 固定 50，深成指万级点位 `10056.78`(8 字符)在 10px 字号下约 50–56px，被裁切/挤压，y 轴"宽度太窄"。
- 修复 `MarketDetailPage.tsx`：①手机端 `grid.containLabel:true` + `left:8/right:16`，让 ECharts 按最宽 y 标签自动留白，数字永不截断；②y 轴字号手机 10→9，并启用千分位 `toLocaleString`（如 10,056.78）便于读万级点位；③图表高度 `clamp(240,42vw,360)`→`clamp(260,46vw,380)` 更舒展。桌面端 grid.left 保持 65 不变。
- 验证：tsc 详情页零新错；Vite 200。

## 优化：指数详情页 手机端分时图左右留白过多(UI 设计师)
- 用户反馈上一步 y 轴改宽后"表两边留白太多、曲线太窄"。
- 根因：千分位 `10,056.78`(9 字符) + `left:8` 偏保守，containLabel 把整段标签宽算进左留白，曲线区被压窄。
- 修复：①手机端 y 轴 formatter 回归 `toFixed(2)` 去掉千分位(8 字符更窄)、fontSize 9、margin 4；②grid 手机端 `left:8→4 / right:16→10` 收紧边距；③走势图 Card body 手机端 padding `20px 16px → 16px 12px`。containLabel 仍保留(数字不裁切)，绘图区最大化。
- 验证：tsc 详情页零新错；Vite 200。

## 优化：指数详情页 手机端分时图左右零留白(UI 设计师)
- 用户最终要求"曲线区更宽，左右不要留白"。
- 方案：手机端 y 轴标签 `axisLabel.inside:true` 移入绘图区内部(贴左边缘内侧)，并加半透明气泡背景(亮 rgba(255,255,255,.6)/暗 rgba(17,24,39,.6))浮在曲线上仍可读；grid 手机端 `left:0 / right:0`、`containLabel:false`，曲线占满整宽、零留白。颜色选更深(#475569/#CBD5E1)保证气泡上可读。
- 桌面端保持原样(inside:false、left:65、right:25、外部标签)。top/bottom 手机端略降(16/32-52)给曲线更多纵向空间。
- 验证：tsc 详情页零新错；Vite 200。

## 优化：指数详情页 分时图「全加」5 项专业功能（手机端）
- 用户确认一次性把上一轮结尾列出的 5 项全部加到手机端分时图：`MarketDetailPage.tsx` 改造：
  1. **昨收基准线**：`markLine` 加 `yAxis: prevClose`（`prevClose = currentIndex.point - currentIndex.change`，前端派生无需后端），formatter `昨收 X.XX`，虚线。
  2. **右侧最新价标签**：主 line 的 `markPoint` 末端加 `coord:[lastValidIndex,lastValidPrice]`、`position:'right'`、涨跌色背景白字（lastValid 取曲线最后一个有效点，避免未来空段拖到图最右）。
  3. **整列刻度带**：y 轴 `splitArea.show:isMobile` 整列淡色刻度带替代独立气泡（原 zero 留白方案的气泡改为背景带）。
  4. **成交量副图**：`hasVolume` 守卫，仅当真实有量才显示；新增第二 grid(xAxisIndex:1/yAxisIndex:1)，bar 按 `p>=prevClose` 涨红跌绿、`barWidth:'55%'`；`axisPointer.link:[{xAxisIndex:'all'}]` 主副图十字联动；图表高度随 `hasVolume` 升到 `clamp(300px,60vw,480px)`。
  5. **触摸跟手十字**：手机端 tooltip `triggerOn:'mousemove|touchstart|click'` + `axisPointer.snap:true`、彩色 dashed 十字、`z:100`。
- `indexService.ts`：`IntradayData` 接口新增 `volumes?: number[]`；`fetchIntradayData` 透传 `res.data.data.volumes`。

## 修复：分时成交量解析错取成交额 + 累计未转增量（后端）
- 根因：`parseTencentMinuteData` 取 `parts[parts.length-1]`（末段）= 成交额(元，如 90亿)，非成交量；且腾讯分钟量是**当日累计值**，直接画柱会成单调楼梯。
- 真实格式（已 curl 原始 `web.ifzq.gtimg.cn/.../minute/query` 验证）：每行 `时间 价格 成交量(手) 成交额(元)` 共 4 段，242 行稳定。`minuteData` 结构为 `{data:{data:[...]}}` 双层嵌套（走 `Array.isArray(minuteData.data)` 分支）。
- 修复：成交量取 `parts[2]`（累计），并 `prevCumVol` 转**逐分钟增量**（`vol = cumVol - prevCumVol`，首根=首值），副图呈真实量能分布。
- 验证（`000001` 重启清缓存后）：API 返回 `data keys:['times','prices','volumes']`，volumes 242 个；首 5 根 `[4763106,13215484,13432661,8279593,7609910]`；午休过渡 1 个 0（1130→1300 无成交，正确）；**增量之和 564,988,582 = 收盘累计总量**，重建精确。
- 坑：旧 server(PID 30768) + `globalCache.json` 持久化缓存命中加 volumes 前的旧值（周日 TTL 12h）→ parser 未调用、探针不写文件、API 无 volumes。必须 `taskkill` 杀旧进程 + 删 `server/data/globalCache.json` + 重启 nodemon 才生效。K线回退 `kline[5]`、东财 `parts[5]` 成交量位置本来正确，未改。
- 验证：tsc 本次零新错（仅预存 Header/PortfolioPage 的 `SettingsData.settings` 无关错）；Vite(5173) 在线 200；后端 3001 重启后 `tencent_minute` 正常返回 volumes。

## 修复：手机端分时图 主图与副图(成交量的)网格重叠（UI Designer）
- 现象：手机端「x轴和折线图相交」——实为双 grid（主图+成交量副图）边界用「像素+百分比混搭」硬凑：主图 `bottom:82`(px) + 副图 `top:'74%'`(百分比)。图表偏高(≈380–480px)时主图底边(298)>副图顶边(281)，重叠 ~17px，主折线压到副图量柱与时间轴。
- 修复 `MarketDetailPage.tsx` grid：主图有副图时 `bottom:'32%'`；副图 `top:'70%' / bottom:28`。百分比锚定使主图恒在 `[16px,68%H]`、副图在 `[70%H,H-28px]`，任意机型高度零重叠，且副图底部 28px 稳定容纳时间轴标签。无副图时主图 `bottom:52`(px) 不变。
- 验证：tsc 零新错；Vite 200。
