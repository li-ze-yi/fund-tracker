# 2026-08-10 工作日志

## 手机端分时图 y 轴与折线相交 — 修复
- 现象：用户更正（非 x 轴）— 手机端左侧价格刻度与折线在左边缘相交。
- 根因：`grid.left:0` + `yAxis.axisLabel.inside:true`(零留白设计) 把价格数字画进绘图区左缘；x 轴 `boundaryGap:false` 使 09:30 首点锚在 x=0，开盘价≈昨收(纵向中段) 正好撞上左侧中段刻度 → 折线穿过数字。
- 修复（`web/src/pages/market/MarketDetailPage.tsx` 主 yAxis）：
  - `z: isMobile ? 10 : 0` 把左侧刻度抬到折线之上；
  - `splitNumber: isMobile ? 4 : 5` 降密度；
  - `axisLabel.backgroundColor`(主题半透明 0.82) + `padding:[1,3]` + `borderRadius:2` 给刻度加浮层底片，折线穿过被遮住、数字始终清晰。
  - 保留零留白（inside:true / left:0 不变）。
- 备选方案（未做，用户可选）：把刻度挪回 `inside:false` 外侧 + `grid.left:34` 做硬隔离，代价是左侧多 ~34px 留白，与零留白诉求相悖。
- 验证：tsc 零新错（仅预存 Header/PortfolioPage `settings` 无关错）；Vite(5173) 200，HMR 生效。

## 12:44 左槽再收窄
- 用户要左侧留白再少一点。手机端 y 轴左槽 44px → **36px**（主图+副图网格同步）。
- 安全依据：y 轴刻度已改整数(`toFixed(0)`，"3900" 比 "3900.00" 更短)，36px 仍不裁切标签。
- 验证：tsc 零新错；Vite 200。

## y 轴刻度去小数位
- 用户要求 y 轴不显示小位数(小数)。主 yAxis `axisLabel.formatter` 由 `v.toFixed(2)` 改为 `v.toFixed(0)`，刻度显示整数(如 3900 而非 3900.00)。
- 注意：最新价浮标(L352 `lastValidPrice.toFixed(2)`)与昨收标签(L376 `prevClose.toFixed(2)`)仍保留 .00，用户仅要求 y 轴；如需统一去小数可再改。
- 验证：tsc 零新错；Vite 200。

## 用户要求进一步压缩右侧：最新价标签改为图内浮标
- 用户回复"需要"（采纳上轮提出的更紧凑方案）。
- 改动：
  - 主图 grid 手机端 `right` 由 48 降回 `8`；副图 grid `right` 同步 4→8，与主图对齐。
  - 最新价 markPoint 标签：手机端 `position:'right'` → `insideTop` + `offset:[-24,0]`（浮在折线末端上方并左移，避免右缘裁切）；桌面端保持 `right`。
- 效果：右侧不再有 48px 标签列，图表近乎贴边（仅 8px）；最新价以图内浮标显示。
- 验证：tsc 零新错；Vite 200。

## 走势图容器多重留白收紧（用户反馈：容器到屏幕右间隔 + y轴到容器还有间隔）
- 根因：页面包裹层 `padding:'20px 16px'`(L449) + 走势图 Card `body:'16px 12px'`(L619) 叠加，图表离屏幕左右各 ~28px；再叠加图表内 `left:44`/`right:4`。
- 修复：
  - 走势图 Card 手机端 `marginLeft/Right: -16` 破出页面左右 padding 做整屏通栏，边框贴屏幕边缘；`body` 横向 padding 归零 (`16px 0`)。
  - 图表主 grid 手机端 `right` 由 4 提至 48：预留"最新价"标签列(约45px宽)，避免通栏后末端标签被屏幕裁切（该裁切此前被卡片 padding 勉强掩盖，是潜在 bug）。
  - `left:44`(方案二 y轴左槽，必需) 保留。
- 效果：容器到屏幕的冗余 padding 已消除；左侧仅剩方案二必需的 44px y轴标签列，右侧为 48px 最新价标签列（与左列对称，内容非空白）。
- 验证：tsc 零新错；Vite 200。

## 用户改选 方案二（细左槽硬隔离）
- 00:31 用户明确"方案二执行"。撤回浮层方案，改为硬隔离：
  - 主 yAxis `axisLabel.inside` 由 `isMobile`(true) 改回 `false`（刻度移回绘图区外侧）；移除 `z:10` 与半透明底片(backgroundColor/padding/borderRadius)。
  - 两块 grid 的 `left` 由 `isMobile?0:65` 改为 `isMobile?44:65`（手机端 ~44px 左槽容纳价格刻度）。
  - 取舍：左侧重新出现 ~44px 留白，换取折线永远不碰刻度（物理隔离）。
- 验证：tsc 零新错；Vite 200。
