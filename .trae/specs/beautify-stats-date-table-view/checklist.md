- [x] 新分支 `feature/stats-date-table-view` 已从 `main` 创建，工作区干净
- [x] `/stats/daily` 接口支持 `?year=YYYY&month=MM` 查询参数，无参数时保持最近 30 天行为
- [x] `statsService.getDailyStats` 支持可选 `{ year, month }` 参数
- [x] 统计概览卡片 6 个指标项呈现对称网格布局，每项有独立子卡片背景与圆角
- [x] 概览卡片数值使用 `--font-mono`、`letter-spacing: -0.02em`、`+/-` 前缀
- [x] 视图模式 `Segmented` 控件已添加，默认 `chart`，可在 `chart` / `date_table` 间切换
- [x] `date_table` 模式下周期选择器（日/月/年）隐藏或禁用
- [x] 日历网格按月份分段，7 列（周一~周日）× 5~6 行，日期对齐正确星期列
- [x] 单元格颜色按收益幅度分 4 档（<0.5%、0.5%-1%、1%-2%、>2%），正负使用 `--gain` / `--loss` 配色
- [x] 单元格悬停/点击显示 tooltip，含日期、收益金额、收益率
- [x] 月份切换（上一月/下一月/返回本月）功能正常
- [x] 隐藏金额开关生效时单元格数值与 tooltip 金额显示 `****`，色块仍着色，收益率不隐藏
- [x] 移动端日历网格单元格最小 36px 宽，不溢出，极窄屏幕支持横向滚动
- [x] 桌面端日历单元格 44px 宽，3 列概览卡片布局正常
- [x] 浅色/深色主题切换后颜色块对比度符合 WCAG AA
- [x] 加载中显示 `Skeleton`，无数据显示"该月暂无收益数据"空状态
- [x] `chart` 模式下原有图表 + 明细表行为保持不变（无回归）
- [x] 动态字号：数字按位数五档缩放（<100/<1k/<10k/<100k/>=100k），桌面端比移动端大 2px
- [x] 金额/收益率切换控件已添加，切换单元格主显示内容，tooltip 始终完整
- [x] 移动端概览卡片 3 列紧凑布局（padding 8px、label 9px、value clamp 11-14px 允许换行）
- [x] 粒度选择器为「日 / 月 / 年」三档，默认「日」
- [x] 日视图：当月日历网格（7 列），支持月份切换
- [x] 月视图：12 个月网格（4×3），支持年份切换，点击某月跳转日视图
- [x] 年视图：7 年网格（3 列），支持 7 年步进翻页，点击某年跳转月视图
- [x] `/stats/monthly` 接口支持 `?year=YYYY` 参数
- [x] `statsService.getMonthlyStats` 支持可选 `{ year }` 参数
- [x] 无数据时日期表格渲染空白方格（不显示 Empty 空状态）
